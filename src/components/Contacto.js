import React, { Component } from 'react'

export default class Footer extends Component {
    render() {
      return (
        <div id="contacto">
            <p>
                Formulario Mensaje / Evento / Blog
            </p>
        </div>
      );
    }
  }